using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public abstract class BaseHeroBehaviour : MonoBehaviour
{
    public virtual void PrimaryAttackEnemy(EnemyBehaviour enemy) // abstract instead of virtual
    {
        
    }
    public virtual void SecondaryAttackEnemy(EnemyBehaviour enemy) // abstract instead of virtual
    {
        
    }
    public virtual void Skill_1AgainstEnemy(EnemyBehaviour enemy) // abstract instead of virtual
    {
        
    }
    public virtual void Skill_2AgainstEnemy(EnemyBehaviour enemy) // abstract instead of virtual
    {
        
    }
    public virtual void Skill_3AgainstEnemy(EnemyBehaviour enemy) // abstract instead of virtual
    {
        
    }
    public virtual void Skill_4AgainstEnemy(EnemyBehaviour enemy) // abstract instead of virtual
    {
        
    }
    public virtual void Skill_5AgainstEnemy(EnemyBehaviour enemy) // abstract instead of virtual
    {
        
    }
    public virtual void Skill_6AgainstEnemy(EnemyBehaviour enemy) // abstract instead of virtual
    {
        
    }
    public virtual void Skill_7AgainstEnemy(EnemyBehaviour enemy) // abstract instead of virtual
    {
        
    }
    public virtual void Skill_8AgainstEnemy(EnemyBehaviour enemy) // abstract instead of virtual
    {
        
    }
}
